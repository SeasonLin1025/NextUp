# NextUp 后续计划

> 本文件记录已规划但本次未实现的功能，供后续迭代参考。

---

## 1. 时间切片建议

**背景**：
当推荐任务耗时较长（如 2 小时），或当前推荐任务的时间缺口很大时，用户可能会把所有时间投入该任务，导致另一个"临界但可完成"（Tier 3）的任务被"饿死"——虽然本来可以完成，但因为没及时切换而错过截止时间。

**计划实现**：
- 检测任务列表中是否存在 Tier 3 任务（临界但可完成），且其截止时间早于当前推荐任务完成预期时间
- 若存在，在大卡下方增加提示：「建议 XX:XX 前切换到「任务 B」，还来得及」
- 切换时间点 = now + 当前 Tier 3 任务的 deadline - 安全缓冲（如 10 分钟）

---

## 2. 紧急队列模式

**背景**：
当前大卡底部在存在多个高风险任务（slack < 0）时，会显示"还有 N 项任务也已时间不足"的静态文字提示，不可点击，用户无法查看具体是哪些任务以及建议处理顺序。

**计划实现**：
- 将该提示改为可点击按钮，点击后展开「紧急队列」视图
- 视图内列出所有 slack < 0 的任务，按 `sortActiveTasksByRisk` 排序
- 每项显示任务名、riskLabel、时间缺口
- 支持直接在队列中勾选完成或跳转编辑
- 可考虑轮播大卡模式：左右滑动切换多个高风险任务推荐
